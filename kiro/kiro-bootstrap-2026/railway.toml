[build]
builder = "nixpacks"
buildCommand = "corepack enable && pnpm install --force && npx prisma generate --schema=packages/database/prisma/schema.prisma && pnpm --filter @community-os/api build"

[deploy]
startCommand = "node apps/api/dist/apps/api/src/main.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[service]
internalPort = 4000
